-- set colorscheme to nightfly with protected call
-- in case it isn't install
vim.cmd([[colorscheme catppuccin-macchiato]])
-- if not status then
-- print("Colorscheme not found!") -- print error if colorscheme not installed
-- return
-- end

